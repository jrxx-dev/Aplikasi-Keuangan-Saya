{
    "name": "financemy-workspace",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "node start-dev.js",
        "install:all": "cd finance-app && npm install && cd ../main-dashboard && npm install"
    }
}